// 函数抽离

//  移除组件
export var studentRemoveDomain = (obj, item, val) => {
  if (val === 'hostelNewNum') {
    const index = obj.changeOneInput.hostelNewNum.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.hostelNewNum.splice(index, 1)
    }
  }
  if (val === 'stuNum') {
    const index = obj.changeOneInput.stuNum.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.stuNum.splice(index, 1)
    }
  }
  if (val === 'stuName') {
    const index = obj.changeOneInput.stuName.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.stuName.splice(index, 1)
    }
  }
  if (val === 'oldHostelName') {
    const index = obj.changeOneInput.oldHostelName.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.oldHostelName.splice(index, 1)
    }
  }
  if (val === 'oldFloorName') {
    const index = obj.changeOneInput.oldFloorName.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.oldFloorName.splice(index, 1)
    }
  }
  if (val === 'newHostelName') {
    const index = obj.changeOneInput.newHostelName.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.newHostelName.splice(index, 1)
    }
  }
  if (val === 'newFloorName') {
    const index = obj.changeOneInput.newFloorName.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.newFloorName.splice(index, 1)
    }
  }
  if (val === 'detail') {
    const index = obj.changeOneInput.detail.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.detail.splice(index, 1)
    }
  }
  if (val === 'hostelNum') {
    const index = obj.changeOneInput.hostelNum.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.hostelNum.splice(index, 1)
    }
  }
  if (val === 'floorManagerAddress') {
    const index = obj.changeOneInput.floorManagerAddress.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.floorManagerAddress.splice(index, 1)
    }
  }
  if (val === 'floorManagerAge') {
    const index = obj.changeOneInput.floorManagerAge.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.floorManagerAge.splice(index, 1)
    }
  }
  if (val === 'floorManagerPhone') {
    const index = obj.changeOneInput.floorManagerPhone.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.floorManagerPhone.splice(index, 1)
    }
  }
  if (val === 'floorManagerSex') {
    const index = obj.changeOneInput.floorManagerSex.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.floorManagerSex.splice(index, 1)
    }
  }
  if (val === 'floorManagerNum') {
    const index = obj.changeOneInput.floorManagerNum.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.floorManagerNum.splice(index, 1)
    }
  }
  if (val === 'floorManager') {
    const index = obj.changeOneInput.floorManager.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.floorManager.splice(index, 1)
    }
  }
  if (val === 'floorName') {
    const index = obj.changeOneInput.floorName.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.floorName.splice(index, 1)
    }
  }
  if (val === 'floorDetail') {
    const index = obj.changeOneInput.floorDetail.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.floorDetail.splice(index, 1)
    }
  }
  if (val === 'stuName') {
    const index = obj.changeOneInput.stuName.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.stuName.splice(index, 1)
    }
  }
  if (val === 'stuNum') {
    const index = obj.changeOneInput.stuNum.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.stuName.splice(index, 1)
    }
  }
  if (val === 'stuProfession') {
    const index = obj.changeOneInput.stuProfession.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.stuName.splice(index, 1)
    }
  }
  if (val === 'stuClass') {
    const index = obj.changeOneInput.stuClass.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.stuClass.splice(index, 1)
    }
  }
  if (val === 'stuSess') {
    const index = obj.changeOneInput.stuSess.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.stuSess.splice(index, 1)
    }
  }
  if (val === 'stuPhoneNumber') {
    const index = obj.changeOneInput.stuPhoneNumber.indexOf(item)
    if (index !== -1) {
      obj.changeOneInput.stuPhoneNumber.splice(index, 1)
    }
  }
}
